# Node npm Example

```sh
ctg run .
```

Clone To Green detects `package.json`, prefers `npm ci` when `package-lock.json` exists, then runs `npm run build` and `npm test` when those scripts are present.
